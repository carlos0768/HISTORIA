# 08. AI アーキテクチャ・原価・品質保証

> 対象: HISTORIA MVP / 状態: 確定（v0.3 で全面改訂） / 最終更新: 2026-09-01
>
> **v0.2 からの変更**: 共有カタログ方式を廃止し、教材と設問をユーザーごとに生成する方針に変更した。
> **モデルは二層構成とする。生成は Gemini API の無料枠、検証は課金モデル**（§2）。
> 変更の理由は作者の指示であり、以下はその前提で最良を設計したものである。

## 0. v0.1 の矛盾の解消

v0.1 は「AIプロバイダー：GCP／使用モデル：Claude,ChatGPT」と書いていた。
**GCP Vertex AI に OpenAI の GPT 系商用 API は無い**ため、この2つは両立しない。

作者の指示（予算を Google の無料枠に収める）を踏まえ、次のように確定する。

## 1. 決定: Gemini API の無料枠を使う

### 1.1 「GCP の無料枠」で LLM が使えるのはどこか

**これは間違えやすい。** Google の生成AIには課金経路が2系統ある。

| 経路 | 無料枠 | 備考 |
|---|---|---|
| **Gemini API（AI Studio 系 / `generativelanguage.googleapis.com`）** | **あり（無期限・カード不要）** | 本プロジェクトが使うのはこれ |
| Vertex AI（`aiplatform.googleapis.com`） | **なし** | 生成モデルは常時課金。Claude on Vertex は Anthropic と同レート |
| GCP $300 無料トライアルクレジット | 新規アカウント90日限定 | 恒久的な「無料枠」ではない |

**決定: 生成に Gemini API の無料枠（Flash 系）を使う。Vertex AI は経由しない。**
**検証（ファクトチェックの二次照合・judge・診断プールの生成）には課金モデルを使う**（§2.1）。

### 1.2 無料枠の実態（2026年時点・**要検証**）

| 項目 | 値 |
|---|---|
| RPM（1分あたりリクエスト数） | **15** |
| RPD（1日あたりリクエスト数） | **1,500** |
| TPM（1分あたりトークン数） | **1,000,000** |
| 対象モデル | **Flash 系のみ**。Pro は2026年4月に無料枠から除外され有料のみ |
| クレジットカード | 不要 |
| 有効期限 | なし |
| **データの扱い** | **Google の製品改善（機械学習を含む）に使われる。人間のレビュアーが入力・出力に注釈を付けることがある**（§4） |

正確な数値とモデルIDは実装時に公式ドキュメントで確認すること（**要検証**）。
TPD（1日あたりトークン数）の上限があるかも確認が必要である（§3.3）。

### 1.3 この選択が持ち込む3つのリスク

| # | リスク | 対処 |
|---|---|---|
| R1 | **無料枠は予告なく変わる。** Pro が2026年4月に外された実績がある | プロバイダ抽象層を1枚置き、差し替え可能にする（§6） |
| R2 | **データが製品改善に使われる。** 未成年の成績プロファイルを扱う本アプリでは重大 | プロンプトから個人識別情報を完全に除去する（§4） |
| R3 | **Flash 系は事実精度が上位モデルより劣る可能性がある。** かつ毎回生成のため人手レビューが不可能 | 正典マスタとの機械照合を厚くし（§5 層2）、**検証だけ別系統の課金モデルに払う**（§2.1） |

## 2. モデル配置（二層構成）

**重い生成は無料枠で回し、安いが効く検証だけに金を払う。**
（★ これは v0.3 当時の方針。2026-09-04 に**両方とも課金**へ変えた。下の注記を参照）

> ### ★ 2026-09-04: 向きを入れ替えた
>
> **生成 = Claude Opus 5（課金） / 検証 = Gemini 3 Pro（課金）** に変更した（作者の判断）。
> v0.3 の「生成は Gemini の無料枠、検証は Anthropic」から**逆**になっている。
>
> 理由は前提が消えたこと。2026-09-02 の実測で、**この鍵からは無料枠の存在を確認できなかった**
> （`gemini-2.5-flash` が 404、他は「前払いクレジットが尽きた」の 429。docs/14 M28）。
> 無料枠が無いなら「生成は0円だから検証に払う」という §2.1 の論法は成り立たない。
> 両方が課金なら、**払う先は品質が効くほう＝生成**である。
>
> 費用は全75節で約 **¥4,300**（月上限 1万円の 43%）。§3.4 に内訳を置いた。
> 以下 §2.1 は**当時の推論**として残す。結論は上の注記が上書きする。

| 用途 | モデル | 課金 | 頻度 | 出力規模 |
|---|---|---|---|---|
| **教材＋設問の生成** | **Claude Opus 5** | **課金** | 1ユニット1回 | 約10,000トークン |
| 範囲の自然文解釈 | Gemini Flash 系 | 課金（小） | 特訓作成時 | 数百トークン |
| **ファクトチェックの二次照合** | **Gemini 3 Pro** | **課金** | 1ユニット1回 | 400トークン |
| **診断プール240問の生成** | **Claude Opus 5** | **課金** | 一度きり | 185,000トークン |
| **LLM-as-judge（開発時のみ）** | **Gemini 3 Pro** | **課金** | プロンプト改善時 | 600トークン |
| 弱点判定 | **LLM を使わない** | — | — | SQL（`04-weakness-engine.md` §7） |
| 確認テストの採点 | **LLM を使わない** | — | — | DB内で完結 |

★ **生成と検証は必ず別プロバイダにする。**この不変条件は §6 のままで、
`lib/ai/client.ts` の `assertConfig` が起動時に強制する。向きが逆になっても
「同一系統による自己検証への退化」を禁じる理由は変わらない。

### 2.1 なぜ検証にだけ払うのか

v0.3 の当初案（全部無料枠）には**構造的な弱点**があった。

> 無料枠で使えるのは Flash 系のみで別系統のモデルを選べないため、
> 「別モデルによる採点」は実現できない。同一モデルである以上、同じ誤りを見逃す可能性は残る。

**この制約が、月あたり数百円で外れる。**

| 論点 | 内容 |
|---|---|
| **単価が2桁違う** | 生成は出力12,000トークン。検証は入力1,200・出力400トークン。**検証は生成の約1/20のコスト** |
| **訓練系統が変わる** | Google と Anthropic では学習データも手法も違う。**Flash が犯した誤りを見逃しにくい** |
| **人手レビューの代替になる** | 毎回生成では人手レビューが不可能（§5）。機械照合と別系統モデルが唯一の防衛線である |

**「品質に金を払う」なら、生成ではなく検証に払う方が費用対効果が高い。**
生成の品質を上げても誤りはゼロにならないが、検証を別系統にすると**見逃しの相関が下がる**。

### 2.2 モデルの選定（2026-09-04）

| | モデル | 単価（USD/MTok） | なぜ |
|---|---|---|---|
| 生成 | **Claude Opus 5** | $5 / $25 | 費用の約9割は出力であり、**払った金がそのまま教材の質になるのは生成側だけ**。全75節で約 ¥4,300 と、上限の半分以下に収まる |
| 検証 | **Gemini 3 Pro** | $2 / $12（**未実測**） | 「この主張は史実か」の判定は短く定型的で、上位モデルを置く必要が無い。誤りの大半は層2の機械照合（正典 1,180件）が先に落とす |

★ **検証を安いほうに置いても、防衛線は薄くならない。** §5 の5層のうち、
層3（このモデル）は層2を通り抜けたものだけを見る保険であり、主役ではない。
別系統であること自体が価値なので、そこは §6 の不変条件が守る。

★ **Gemini 3 Pro の単価とモデル id はまだ実測していない。**
`lib/ai/client.ts` の `PRICES` に Gemini を書いていないのはそのためで、
未知モデルの既定 $3 / $15 に落ちる。Gemini 3 Pro に対しては**過大**なので
遮断器は安全側に働く。実測できたら書く（docs/14 M28）。

★ **Opus 5 は `PRICES` に必ず載せる。** 既定の $3 / $15 は Opus 5 の $5 / $25 を
下回るため、載せ忘れると**元帳が実支出より4割少なく積まれる**。
既定値は「天井」ではない。

### 2.3 送信先が2社になることの帰結

生成は Anthropic、検証は Google へ送る（2026-09-04 に**入れ替わった**）。

| | 送信先 | 送る内容 | 学習に使われるか |
|---|---|---|---|
| 生成 | **Anthropic（課金）** | KC の id・3段階の習得度・単元id | 使われない（有料枠） |
| 検証 | **Google（課金）** | **本文から抽出した歴史的主張のテキストのみ** | 使われない（有料枠） |

★ **入れ替えでプライバシーは悪くなっていない。むしろ良くなった。**
判断材料は「何が行くか」と「学習に使われるか」の2つで、両方とも改善している:

- **重いほう（KC の id と3段階の習得度を運ぶ生成プロンプト）が、無料枠から有料枠へ移った。**
  v0.3 では唯一「学習に使われる」経路だったものが、そうでなくなった
- 軽いほう（検証）は元から個人情報ゼロなので、送信先が変わっても中身は変わらない

**検証プロンプトには KC の id すら不要である。**「ウェストファリア条約は1648年に締結された」
という歴史的主張の文字列だけを送る。**個人に紐づく情報はゼロ**であり、
匿名化の必要すらない。

★ プライバシーポリシー（`10-legal-risk.md` §8b.2）は両社を明記しており、
**社名は変わらないが役割が入れ替わる**。「Google へ送るのは学習履歴を含むプロンプト」
という趣旨の記述があれば直す。§4 の匿名化は生成プロンプトに掛かる仕組みなので、
送信先が変わっても**そのまま効く**（`buildGenerationContext` を通さないと
クライアントに渡せないため。§4.2）。

## 3. リクエスト数の試算

コストではなく **RPD / RPM の消費**が制約になる。

### 3.1 1ユニットあたりのリクエスト数

```
教材＋設問の生成      1 リクエスト
ファクトチェック照合   1 リクエスト（照合できない claims がある場合のみ）
─────────────────────
合計                  2 リクエスト
```

### 3.2 1日あたりの消費

| 想定 | ユニット/日 | リクエスト/日 | RPD 1,500 に対して |
|---|---|---|---|
| 5人 × 1特訓 × 1ユニット/日 | 5 | 10 | **0.7%** |
| 10人 × 3特訓並行 | 30 | 60 | **4.0%** |
| 10人 × 3特訓 ＋ 初回の一括生成（1人10ユニット） | 130 | 260 | **17.3%** |

**無料枠の RPD には十分な余裕がある。** 上限に近づくのは、
10人が同時期に特訓を新規作成して大量の先読み生成が走る場合だけである。

### 3.3 トークンの試算

| 項目 | トークン |
|---|---|
| 出力（本文3,500字＋FC12枚＋四択8問＋claims 20件） | 12,168 |
| 入力（システム＋候補KC 30件＋単元＋弱点情報） | 2,700 |
| **1ユニット合計** | **14,868** |

- 10人 × 3特訓 = 30ユニット/日 → **約 446,000 トークン/日**
- バースト時でも生成は1ユニット110〜180秒かかるため、TPM 1,000,000 には届かない

**要検証**: 無料枠に TPD（1日あたりトークン数）の上限があるか。
あるなら 446,000 トークン/日 が収まるかを実装前に確認する。

### 3.4 原価（2026-09-04 改訂・生成 Opus 5 / 検証 Gemini 3 Pro）

**生成も課金になった。**以下は 1USD = 160円（`lib/ai/budget.ts` の `JPY_PER_USD`）で換算。
元帳には `SAFETY_MARGIN = 1.05` が掛かるので、DB 上の数字は下表の 1.05 倍で載る。

#### 教材1本の実効トークン

| | 入力 | 出力 | 根拠 |
|---|---|---|---|
| 生成 | 5,500 | 10,000 | `prompts/material_v2.md` 3,171字 ＋ KC 12件 ＋ 出力スキーマ 2,151字 / 本文3,500字＋FC12枚＋四択8問＋claims20件 |
| （参考）遮断器の予約 | 約3,000 | 16,000 | `client.ts` の `maxIn = (system+user)/1.5 + 1000` と `MATERIAL_MAX_OUTPUT_TOKENS`。**予約は上限であって実額ではない**。`settle` が実測の usage で確定する。`scripts/measure/render-prompt.ts` が単元ごとの値を出す（wh.4.1.3 で 2,996） |
| 文字数の作り直し | ×1.2 | ×1.2 | `MAX_LENGTH_RETRIES = 1` を平均で織り込む |
| 検証 | 3,500 | 1,500 | claims の JSON と判定 |
| **合計** | **約10,000** | **約13,500** | |

**費用の約9割が出力側である。**（Opus 5 で入力 $0.05 に対し出力 $0.34）
効く手は「モデル選択」と「出力を短くすること」だけで、入力側の工夫はほぼ効かない。
プロンプトキャッシュで削れるのは全体の 2.7% しかないため、入れていない。

#### 全範囲の原価

| 構成 | 教材1本 | **全75節** | 参考: 10人が全員個別化（750本） |
|---|---|---|---|
| **生成 Opus 5 / 検証 Gemini 3 Pro**（採用） | **約 ¥57** | **約 ¥4,300** | 約 ¥43,000 |
| 生成 Sonnet 5 / 検証 Gemini 3 Pro | 約 ¥25 | 約 ¥1,900 | 約 ¥19,000 |
| 生成 Gemini 3 Pro / 検証 Sonnet 5 | 約 ¥29 | 約 ¥2,200 | 約 ¥22,000 |

★ **分母は 75 であって 750 ではない。** `lib/pipeline/generate.ts` の共有教材の短絡
（`isDefaultContext` が真なら `material.user_id IS NULL` で保存し、次の利用者は
生成せずに読む）があるため、**最初の1人が作った教材を残りが無料で読む**。
個別生成に落ちるのは、学習が進んで band が mid/high に上がった単元だけである。
ここを取り違えると10倍の見積りになる。

★ **`syllabus_unit` は 117 件だが、KC を持つ節は 75 件**。生成の対象はこの 75。

**予算1万円に対して、全範囲を1周して 43%。** 一度きりの支出であり、月額ではない
（同じ単元を作り直すのは `force` を使ったときだけ）。

★ **Batch API を使えば半額（約 ¥2,150）になる。** 教材生成に即時性は要らないので
筋は良いが、`lib/ai/*` が同期呼び出しなので実装が要る。上限の 43% に収まっている
いま急ぐ理由が無いため、やっていない。

### 3.5 無料枠が使えなくなった場合の原価（退避計画）

R1 に備え、有料に落ちた場合の金額を算出しておく。

| 退避先 | 1ユニット | 10人 × 3特訓 × 30日（900ユニット） |
|---|---|---|
| Gemini 有料（Flash 系） | 約 2〜4円（**要検証**） | 1,800〜3,600円/月 |
| Claude Sonnet 5（$2/$10 per MTok・1USD=150円） | 約 18.9円 | **17,010円/月** ← 予算超過 |
| Claude Sonnet 5 ＋ 非同期バッチ（約50%割引） | 約 9.5円 | 8,550円/月 |

**この判断は今は行わない（作者の決定）。** 無料枠の条件変更が実際に起きた時点で、
下記を見て決める。Phase 0 では検討しない。

**生成まで Claude に退避すると予算 1万円を超える。** そのときは
(a) 利用者を5人に絞る (b) 教材の文字数を減らす (c) 共有カタログ方式に戻す
のいずれかが必要になる。

**なお (c) が最も安い。** 共有カタログなら400ユニット × 18.9円 = **7,560円の一度きり**
（非同期バッチで50%引きなら3,780円）で以後ほぼ0円になり、**さらに人手レビューもできる**。
「毎回生成 × 上位モデル」は3つの選択肢のなかで最も高くつく組み合わせであり、
品質に金を払うなら共有カタログに戻す方が安全である。この判断は
`14-open-questions.md` Q4 に未決として記録する。

## 4. 無料枠のデータ利用への対処（最重要）

**無料枠に送ったデータは Google の製品改善（機械学習を含む）に使われ、
人間のレビュアーが入力・出力に注釈を付けることがある。**
Google 自身が非有料サービスに機密・個人情報を送らないよう案内している。

HISTORIA は **16歳未満を含む未成年の成績プロファイル**を扱う（`10-legal-risk.md` §5）。
このまま個人の学習データを送るのは、同意を取ったとしても筋が悪い。

### 4.1 決定: プロンプトから個人識別情報を完全に除去する

教材生成に必要なのは「**どの KC が弱いか**」であって「**誰が**」ではない。

```
送ってよいもの:
  - syllabus_unit.id（'wh.2.4.1'）      … 公開されている教科書の章立て
  - kc.id のリスト（'kc.islam.umayyad_vs_abbasid'）… 本アプリが定義した公開語彙
  - 各KCの習得度の帯（'low' / 'mid' / 'high'）      … 数値ではなく3段階に丸める
  - 反復して選ばれた誤選択肢のラベル（'アッバース朝'）… 歴史用語であって個人情報ではない

送ってはいけないもの:
  - user_id / 表示名 / メールアドレス / 生年月日
  - 生の正答率・解答時刻・解答履歴
  - 連続学習日数・累計解答数などの行動ログ
  - 自由入力欄の内容（範囲指定の自然文と、教材の中の「調べる」の検索語を除く。
    後者は 60 字まで・UUID を含まないことを確かめ、送る前に画面でそう告げる。`11` §4.1）
```

送信内容は「**イスラーム史のこの3項目が弱い学習者向けに教材を書け**」という形になり、
特定の個人に紐づかない。

### 4.2 実装で強制する

```
lib/ai/redact.ts
  export function buildGenerationContext(userId): AnonymizedContext

  戻り値の型に user_id / email / birth_date を含めない（型で防ぐ）。
  この関数を通さないコンテキストを LLM クライアントに渡せないようにする
  （クライアントの引数型を AnonymizedContext に限定する）。
```

`AnonymizedContext` を通らない経路を作らないこと。これに対するテストを書き、
個人識別情報がプロンプトに混入しないことを固定する。

### 4.3 プライバシーポリシーに明記する

> **学習内容の生成のため**、あなたが苦手としている単元・項目の識別子を Google（Gemini API）へ送信します。
> 氏名・メールアドレス・生年月日・解答履歴そのものは送信しません。
> 無料枠を利用しているため、送信された内容は Google のサービス改善に利用される場合があります。
>
> **生成された文章の事実確認のため**、教材から抽出した歴史的な記述（「◯◯条約は△△年に締結された」等）を
> Anthropic へ送信します。この送信にあなたに関する情報は一切含まれません。
> 有料の枠を利用しているため、送信された内容がモデルの学習に使われることはありません。
>
> **教材の中で語を調べるとき**、あなたが入れた検索語だけを Google（Gemini API）へ送信します
> （意味の近い項目を引くためです）。誰が調べたかは送信しません。

**この一文を書けない設計にしない。** 書けるように §4.1 を守る。

## 5. ハルシネーション対策 — 5層（毎回生成に合わせて再設計）

世界史は年号・人名・因果が命であり、**受験生は誤りを検知できない読者**である。

v0.2 では共有カタログ400件を人手レビューする前提だったが、
**毎回生成では人手レビューが物理的に不可能になる**（生成量がレビュー容量を無限に超える）。
さらに Flash 系は上位モデルより事実精度が劣る可能性がある。

**したがって機械照合が唯一の防衛線になる。ここを厚くする以外に手はない。**

### 層1: 生成時に主張を構造化出力させる

`07-content-pipeline.md` §5.3 の `claims` 配列。本文と同時に
「本文中の検証可能な主張（年号・人物・出来事・因果・地名）」を strict JSON で吐かせる。

### 層2: 正典マスタとの機械照合（**この層の重要度が跳ね上がった**）

```sql
CREATE TABLE canon_event (
  id         text PRIMARY KEY,
  label      text NOT NULL,
  aliases    text[] NOT NULL DEFAULT '{}',
  year_from  int NOT NULL,
  year_to     int,
  precision  text NOT NULL CHECK (precision IN ('exact','decade','century')),
  region_ids smallint[] NOT NULL DEFAULT '{}'
);
```

```
kind='year' の claim について:
  claim.subject を canon_event.label / aliases と照合
  → 年が食い違えば FLAG
  → 照合できなかったら層3へ
kind='person' の claim は person テーブルと照合
```

**正典マスタの規模は約1,200件から始める**（作者判断・2026-09-02）。

v0.3 のこの節は「人手レビューが無くなった分を埋める」として 3,000件としていたが、
それは実測ではなく見積りである。1単元の生成が出す claims は 12〜24件なので、
**75節 × 約16件 ≒ 1,200件**で照合対象が claims と同じ桁に乗る。
まず1,200件で照合率を実測し、**足りない分を実測で足す**。
`person` も同時に約450件入れる（下記のとおり照合率の分母に person claim が入るため、
person が0件だと canon_event を何件足しても目標に届かない）。

照合率の目標: **教材中の `kind='year'` と `kind='person'` の claim の 80% 以上が機械照合できること。**
Phase 0 で実測し、下回るならマスタを追加する（`13-roadmap.md`）。

#### 実装で決めたこと（`lib/pipeline/factcheck.ts`）

| 決めごと | 理由 |
|---|---|
| **照合は `subject` を優先し、無いときだけ本文で代用する** | 本文全体で部分一致させると「ウェストファリア条約で三十年戦争が終わった」から**三十年戦争の正典**を引いてしまう |
| **年は `claim.year_from` を優先し、本文からの抽出は後詰め** | 正規表現は3〜4桁の西暦と「前N年」しか拾えない。モデルが構造化して返した値の方が確実 |
| **年を読み取れない claim は分母に入れない** | 「前18世紀の…」は正典を何件足しても照合できるようにならない。分母に含めると照合率が構造的に80%へ届かなくなる。**外した件数（`unreadable`）は別に数えて出す**（黙って捨てない） |
| **最長一致・同点は id 順（`ORDER BY`）** | `唐` のような短いラベルが `唐宋変革` を食う。`LIMIT 1` だけだと当たる行が実行ごとに変わりうる |
| **`wrong` の理由に正典の id を書く** | 年号は一括承認で入れており検算していない。**誤っているのが教材ではなく正典の側**でありうるので、`blocked_reason` から辿って正典を直せるようにする |
| **`event` を `causal` に畳まない** | 生成側のスキーマは `event` を出す。畳むと層3へ渡す種別が事実と食い違う |

### 層3: **別系統の課金モデル**による二次照合

層2で照合できなかった claim だけを、**生成に使ったのとは別系統のモデル**へ、
**生成時のコンテキストを渡さずに**問い直す。

```
生成: Gemini Flash 系（無料枠）
検証: Claude Sonnet 5（課金）        ← 訓練系統が違う

入力: 「次の記述は史実として正しいか。前後の文脈は与えない」＋ claim のリスト（1,200トークン）
出力: 各 claim に { verdict: 'correct'|'incorrect'|'uncertain', reason }（400トークン）
単価: 0.96円/ユニット
```

**これが人手レビューの喪失に対する主たる代替手段である。**
同一モデルの自己検証では同じ誤りを見逃すが、Google と Anthropic では
学習データも手法も違うため**見逃しの相関が下がる**。

`incorrect` が1件でも出たら層5へ。`uncertain` が3件以上でも同様。

### 層4: ユーザー報告導線（**毎回生成では最後の砦**）

全教材ページ・全問題に「この記述はおかしい」ボタンを置く。

```sql
CREATE TABLE content_report (
  id          bigserial PRIMARY KEY,
  user_id     uuid NOT NULL,
  target_kind text NOT NULL CHECK (target_kind IN ('material_section','item')),
  target_id   uuid NOT NULL,
  comment     text,
  status      text NOT NULL DEFAULT 'open'
              CHECK (status IN ('open','confirmed','dismissed','fixed')),
  created_at  timestamptz NOT NULL DEFAULT now()
);
```

報告が付いた段落は**即座に非表示**にし、再生成キューへ入れる。

毎回生成では同じ誤りが他のユーザーに出るとは限らないため、
**報告は個別の教材の修正にしかならない**。共有カタログなら1件の修正が全員に効いたが、その利点は失われた。
代わりに、報告された claim を `canon_event` に追加する運用にして層2を育てる。

### 層5: 事実確認を通らなかった教材は**配信しない**（作者の決定）

**方針: 誤りが検出されたらエラーにして、その教材ユニットを出さない。**

v0.3 の当初案は「FLAG された段落だけを落として残りを表示する」だったが、
作者の判断でこれを強化した。**部分的に落とした教材は、抜けたまま読まれる。**
受験生は何が省略されたか分からず、因果の連鎖が途切れた文章を読むことになる。

```
生成
  ↓
層2（canon_event 照合）＋ 層3（別系統モデルによる二次照合）
  ↓
FLAG なし        → status = 'ready'      配信する
FLAG あり        → 該当セクションを再生成（自動・最大2回）
  ↓
再生成後も FLAG  → status = 'blocked'    ★ 配信しない
                   blocked_reason に検出内容を記録（作者が見る）
                   ユーザーには「この単元の教材は作れませんでした」
                   → 自学モードに降格し、設問だけを出す（§5.1）
```

FLAG の条件:

| 条件 | 判定 |
|---|---|
| 層3で `incorrect` が **1件以上** | 再生成 → 残れば `blocked` |
| 層3で `uncertain` が **3件以上** | 同上 |
| 層2で年号・人名の不一致が **1件以上** | 同上 |

### 5.1 配信できなかったときのユーザー体験

**教材が出なくても学習は止めない。**

```
「この単元の教材は作れませんでした。教科書で学習してください。」
  ＋ [ 問題演習だけ始める ]   ← 設問は別途評価されるので出せる
  ＋ [ もう一度作ってみる ]   ← 手動での再試行（1日1回まで）
```

設問（`item`）は教材とは独立に評価する。**設問は1問単位で落とせる**ため、
誤りが検出された設問だけを `hidden = true` にし、残りは出題する。
ある KC の設問が全滅した場合は `pick_item()` が `null` を返し、
その KC はその日の出題からスキップされる（`04b-spaced-repetition.md` §5）。

### 5.3 設問（item）の承認経路

**教材は「配信するか / しないか」の二値だが、設問は1問単位で落とせる。**
`item.approved = true` になって初めて出題される。false のままの設問は
`pick_item()` の候補に入らない（`04b-spaced-repetition.md` §5）。

| 種別 | 承認する主体 | `approved_by` | タイミング |
|---|---|---|---|
| **ユーザー生成の設問**（`user_id` 非NULL） | **ファクトチェックのパイプライン（自動）** | `'factcheck'` | 層2・層3 を通過した時点でサーバーが `true` にする |
| **診断プール**（`user_id IS NULL`） | **作者（手動）** | `'author'` | 240問を全件レビューして `true` にする |

```
教材の生成が完了
  ↓
層2（canon_event 照合）＋ 層3（別系統モデルによる二次照合）
  ↓
その設問に紐づく claim に FLAG なし
  → item.approved = true / approved_by = 'factcheck' / approved_at = now()
  ↓
FLAG あり
  → approved = false のまま、hidden = true / hidden_reason = 'factcheck_flag'
     その設問は出題されない。同じ KC の他の設問は出題される
```

**毎回生成では人手レビューが不可能なため、`approved` は自動でしか立たない。**
これは `approved` の意味が v0.2（作者が読んで承認した）から
v0.3（機械検証を通過した）に変わったということである。
`approved_by` を必ず残すのは、後から「どちらの経路で通ったか」を区別するためである。

ある KC の設問が全て `approved = false` になった場合、`pick_item()` は `null` を返し、
その KC はその日の出題からスキップされる。**設問が全滅した KC は
`generation_job(kind='items_refresh')` をキューに入れて作り直す。**

### 5.2 この決定が意味すること

**誤りの混入を「品質問題」から「可用性問題」に変換している。**

| | 当初案（部分省略） | 決定（配信しない） |
|---|---|---|
| 誤りが受験生に届くか | 届きうる（省略されなかった誤りは残る） | **届かない** |
| 誤り率が高いとどうなるか | 品質の低い教材が配られる | **教材が出ない頻度が上がる** |
| 検知できるか | しにくい（誰も気づかない） | **すぐ分かる**（`blocked` の件数を数えるだけ） |

したがって **Phase 0 の判定基準も変わる**（`13-roadmap.md`）。

```
旧: 「20ユニットで事実誤りが3件以下」
新: 「20ユニットのうち配信できるユニットが 17本（85%）以上」
```

誤り率が高くても誤情報は配られない。代わりに「教材が出ない」という形で現れるので、
**その頻度が許容できるかどうかで判断する**。

## 6. プロバイダ抽象層（二層構成の実装と R1 への備え）

無料枠は予告なく変わる。**モデル呼び出しを1箇所に閉じ込める。**

**v0.3 では2プロバイダの併用が常態である**（生成=Gemini / 検証=Anthropic）。
抽象層はもともと退避のために置いたが、日常的に両方を使う。

```
lib/ai/
  client.ts        -- generate(ctx: AnonymizedContext, schema, maxOutputTokens): Promise<T>
                   -- verify(claims: Claim[], maxOutputTokens): Promise<Verdict[]>
  providers/
    gemini.ts      -- 生成用。Gemini API 無料枠
    anthropic.ts   -- 検証用。課金。生成への退避先も兼ねる
  redact.ts        -- AnonymizedContext を組み立てる唯一の入口（§4.2）
  budget.ts        -- 支出遮断器。予約→関門→確定→回収（§7.1）
```

**`maxOutputTokens` は省略可能にしない。** 支出遮断器の見積りは
「入力＋`maxOutputTokens`」を上限として計算するため、これが未指定だと上限が定義できず、
**遮断器が守る不変条件が崩れる**（§7.1）。型の上で必須引数にし、既定値も置かない。

**`client.ts` は `budget.ts` の関門を通ってからしか外に出ない。**
予約が取れなかった呼び出しは `BudgetExceededError` を投げ、呼び出し側は
`generation_job.status = 'cancelled'`（`error = 'budget_halted'`）にして終わる。
リトライしない — 遮断中の再試行は無意味である。

```
GEN_PROVIDER    = 'anthropic' | 'gemini'      -- 生成。既定 anthropic
GEN_MODEL       = モデルID                     -- 既定 claude-opus-5
VERIFY_PROVIDER = 'gemini'    | 'anthropic'   -- 検証。既定 gemini
VERIFY_MODEL    = モデルID                     -- 既定 gemini-3.6-flash（M35 で確定させる）
```

**起動時に落とす設定が2つある**（`assertConfig`）。

1. **生成と検証が同じプロバイダ。** 同一モデルの自己検証に退化するため。
   向きを入れ替えるのは自由だが、2社であることは動かせない
2. **プロバイダとモデル id の食い違い**（`gemini` に `claude-*`、`anthropic` に `gemini-*`）。
   `gemini.ts` は id を `/models/<id>:generateContent` と **URL のパスにそのまま入れる**ので、
   食い違えば 404 になる。**向きを入れ替えるとき片方だけ直し忘れるのが典型で、
   結果は鍵の不具合と見分けがつかない**（M28 で実際に起きた）

**アプリのどこからも `fetch` で直接 LLM を呼ばない。** `client.ts` だけが外に出る。
これにより、無料枠が消えた日に env を1つ変えるだけで退避できる。

## 7. レート制限とリトライ

無料枠は 15 RPM である。**同時実行を絞る。**

| ガード | 実装 |
|---|---|
| 同時生成数 | アプリ全体で **3並列まで**（`generation_job` の `status='running'` を数える） |
| RPM 超過（429） | 指数バックオフ（2s / 4s / 8s）・**最大3回**。SDK の自動リトライは無効化して一元管理する |
| RPD 超過 | その日の生成を停止し、UI に「本日の生成上限に達しました」を出す。**学習機能は止めない**（§8） |
| 冪等キー | `generation_job (user_id, kind, scope_id, params_hash)` に UNIQUE 制約。リロード連打で二重生成しない |
| ユーザー単位の上限 | 1日 **10ユニット**まで。同時 active な特訓は **5本**まで |
| 検証側（Anthropic）の支出上限 | コンソールで **月 3,000円で通知 / 5,000円で停止**。想定は900円弱なので、超えたらバグを疑う |
| **合計支出の上限** | **アプリ側の遮断器で月 10,000円に達したら課金呼び出しを全停止する（§7.1）。** コンソール上限はプロバイダ別なので合計を守れない |

```sql
CREATE TABLE generation_job (
  id            uuid PRIMARY KEY,
  user_id       uuid REFERENCES app_user(id) ON DELETE CASCADE,
  kind          text NOT NULL
                CHECK (kind IN ('material','factcheck','scope_parse')),
  scope_id      text NOT NULL,
  params_hash   text NOT NULL,
  status        text NOT NULL DEFAULT 'queued'
                CHECK (status IN ('queued','running','succeeded','failed','cancelled')),
  attempts      smallint NOT NULL DEFAULT 0,
  provider      text NOT NULL,
  model         text NOT NULL,
  input_tokens  int, output_tokens int,
  error         text,
  created_at    timestamptz NOT NULL DEFAULT now(),
  started_at    timestamptz, finished_at timestamptz,
  UNIQUE (user_id, kind, scope_id, params_hash)
);
CREATE INDEX ON generation_job (status, created_at) WHERE status IN ('queued','running');
```

### 7.1 支出遮断器 — 1万円に達したら即座に止める（作者の決定）

**決定: 月の AI 支出が 10,000円に達した時点で、課金を伴う呼び出しをアプリ側で機械的に停止する。**

#### なぜプロバイダのコンソール上限では足りないのか

§7 の表には「Anthropic のコンソールで 5,000円で停止」と書いていた。これは残すが、**それだけでは要件を満たさない**。

| 理由 | 内容 |
|---|---|
| **合算できない** | 上限はプロバイダごとに設定する。生成が Gemini 有料に退避した場合（§3.5）、`Anthropic 5,000円 + Google 5,000円 = 10,000円` になり得る。**「合計1万円」を守る主体がどこにも居ない** |
| **事後にしか効かない** | コンソール上限は使い切ってから遮断する。**超えてから止まる**のであって、超えないように止まるのではない |
| **アプリが状態を知らない** | 課金が止まっても HISTORIA 側は何も知らず、生成ジョブを投げ続けて全部失敗する。UI にも出せない |

**上限を守る責任をアプリ側に置く。** コンソール上限は「アプリの遮断器が壊れていた場合の二重の網」として残す。

#### 仕組み — 予約・関門・確定・回収

支出は**呼び出しが返ってきて初めて確定する**。したがって確定額だけを足していく元帳では、
**必ず飛行中の呼び出しの分だけ超過する**。これを避けるため、発行前に上限見積りを**予約**する。

```
① 見積り   est = (最大入力トークン × 入力単価 + max_output_tokens × 出力単価) × 為替 × 1.05
② 関門     予約できたら呼び出す。できなければ呼び出さない
③ 確定     応答の usage から実額を計算し、予約を実額に置き換える
④ 回収     確定しないまま15分を過ぎた予約を解放する（プロセス異常終了への備え）
```

**②の関門は SQL 1文である。**

```sql
UPDATE ai_budget
   SET reserved_jpy = reserved_jpy + :est
 WHERE period = :period
   AND NOT halted
   AND settled_jpy + reserved_jpy + :est <= cap_jpy
RETURNING reserved_jpy;
```

**更新行数が 0 なら呼び出しを発行しない。** これが遮断器の全体である。

#### なぜ「SELECT で残高を見て、足りていたら UPDATE」ではないのか

その書き方は競合で破れる。2つのリクエストが同時に残高を読むと、両方が「足りている」と判断して両方が発行する。

単一の `UPDATE` にすると破れない。PostgreSQL の READ COMMITTED では、行ロックの解放を待った文が
**ロック解放後に WHERE 句を新しい行バージョンに対して評価し直す**（EvalPlanQual）。
先行トランザクションが予約した結果が見えた状態で条件を再判定するので、後続は正しく落ちる。

上限10,000円に対して2セッションが同時に6,000円を要求する試験を PostgreSQL 16.13 で実行した。

```
A: granted=1        B: granted=0        最終 reserved_jpy = 6000
```

12,000円にならず、片方だけが通る。**アプリ側のロックもキューも要らない。**

#### 「即座に」の厳密な意味 — 超過額は原理的に 0 円

見積り `est` を**上限**として計算するので、実額が見積りを超えることは無い。したがって

```
settled_jpy + reserved_jpy <= cap_jpy   （常に成立）
```

が不変条件になり、**上限を1円も超えない**。`ai_spend` の
`CHECK (actual_jpy IS NULL OR actual_jpy <= est_jpy)` がこの前提を DB 層で守る。
見積り式にバグがあれば、超過するのではなく**確定の INSERT が落ちる**（＝気づける）。

この不変条件は次の3つに依存する。**どれも実装で強制する。**

| 前提 | 強制の方法 |
|---|---|
| **全ての呼び出しで `max_output_tokens` を指定する** | プロバイダ抽象層（§6）で必須引数にする。指定の無い呼び出しは型で書けない |
| 為替を保守的に見る | `jpy_per_usd = 160` で見積り、`ai_spend` に使用レートを記録する。実勢が円高なら安全側に外れる |
| 未知の課金項目が無い | キャッシュトークン・推論トークン等が単価表に無い形で課金される可能性。**5%の安全余裕**を掛ける（**要検証**: Phase 0 で実請求額と元帳を突き合わせる） |

#### 何が止まり、何が止まらないか

| 対象 | 10,000円到達時 | 理由 |
|---|---|---|
| ファクトチェックの二次照合（課金） | **停止** | 課金呼び出しそのもの |
| judge・診断プール生成（課金） | **停止** | 同上 |
| **教材・設問の生成（無料枠）** | **停止** | 下記 |
| 教材を読む・問題を解く | 継続 | DB だけで完結し、1円もかからない |
| SM-2 の更新・弱点推定・翌日の出題 | 継続 | 同上 |
| 動画の埋め込み再生 | 継続 | 対応付けは事前計算済み（`09b-video.md`） |

**生成が無料でも止める。** 理由は層5（§5）にある。ファクトチェックを通らない教材は
`status='blocked'` になり配信されない。検証が止まっている状態で生成を続けても、
**絶対に `ready` にならない教材を作り続けるだけ**で、無料枠と `generation_job` を浪費し、
ユーザーには「生成中」のまま返らないユニットが溜まる。生成と検証は不可分である。

**閉ループの中核は止まらない。** これは §8 と `12-nonfunctional.md` §2.1 の
「生成が止まっても測定と出題は DB だけで回る」という設計がそのまま効く。
遮断中の UI は「新しい教材の作成を停止しています」を出し、既存の学習は通常どおり続けられる。

#### 段階 — 1万円に到達させない

10,000円は最後の壁であって、そこまで無警告で走らせるべきではない。

| 到達額 | 挙動 | 復帰 |
|---|---|---|
| **5,000円**（`warn_jpy`） | 作者に通知。停止しない | — |
| **8,000円**（`degrade_jpy`） | judge と先読み生成を停止。**ユーザー起点の生成と検証は継続** | 月替わりで自動 |
| **10,000円**（`cap_jpy`） | **課金呼び出しと生成を全停止**（`halted = true`） | **手動のみ** |

8,000円で先に切るのは、そこで止まるのが**開発者都合の処理**（judge・先読み）だけであり、
**利用者の学習体験を落とさずに支出の増加を止められる**ためである。

#### 復帰

- **同月内の自動復帰はしない。** 暴走ループが原因なら自動復帰は同じ支出を繰り返すだけである
- 復帰は管理画面から手動で `halted = false` にするか、`cap_jpy` を引き上げる。どちらも作者のみ
- 月が替われば新しい `period` の行になり、自動的に再開する（前月の行は履歴として残る）

#### 元帳が壊れたときの挙動 — フェイルクローズド

`ai_budget` への UPDATE が失敗した・タイムアウトした・行が無い場合は、
**呼び出しを発行しない**（フェイルオープンにしない）。
「元帳が読めないから止める」で学習が止まることは無い（学習は無料の経路だけで回る）ので、
安全側に倒しても失うものが無い。

#### 元帳と実請求額のずれ（**要検証**）

この遮断器が守るのは**自分で記録した元帳**であって、プロバイダの実請求額ではない。
リトライ・失敗した呼び出し・SDK 内部の再送が元帳に載っていなければ、実請求額の方が大きくなる。

- リトライは §7 のバックオフで一元管理しているので、**リトライも1回ずつ予約・確定する**
- Phase 0 の終わりに、`ai_spend` の合計と実請求額を突き合わせる（`14-open-questions.md` M23）
- ずれが 5% を超えたら安全余裕を引き上げる

## 8. 実行基盤 — 先読み生成が必須になった

### 8.1 v0.2 からの後退

v0.2 では共有カタログを作者がオフラインで作る設計だったため、
**ユーザー起点の長時間生成が存在せず、ジョブ基盤そのものが不要だった**。

毎回生成にしたことで、この利点は失われる。1ユニットの生成は
出力12,000トークン超で **110〜180秒**かかり、Vercel Functions の上限
（Hobby 標準60秒 / Fluid Compute 300秒）に対して危険である。
途中で切れた生成は**課金だけ発生して成果物が残らない**（無料枠では RPD だけ消費する）。

### 8.2 決定: 先読み生成 ＋ ジョブテーブル ＋ ポーリング

**ユーザーを待たせないために、次に読む分を前もって作っておく。**

```
特訓を作成した時点          → 最初の1ユニットを即座にキューへ（この1回だけは待たせる）
ユーザーがアプリを開いた時点 → 「明日以降に読む予定の未生成ユニット」を最大2件キューへ
教材を読み終えた時点        → 次のユニットが未生成ならキューへ
```

締切逆算で「明日何を学ぶか」は決まっている（`05-scheduler.md`）ため、先読みできる。

```
POST /api/materials/generate  → generation_job に enqueue して 202 を返す
GET  /api/materials/:jobId    → status を返す（クライアントは3秒間隔でポーリング）
ワーカー                       → Vercel Function（Fluid Compute・maxDuration 300）で1ジョブ処理
```

**1ジョブ＝1ユニット**にする。複数まとめると300秒を超える。

### 8.3 初回の待ち時間

特訓作成直後の1本目だけは 110〜180秒待たせることになる。UI で正直に見せる
（`11-ux.md` の生成待ち状態）。

```
「教材を作っています（およそ2〜3分）」
＋ 進捗の段階表示（本文 → 設問 → 事実確認）
＋ 「できたら通知する」を選べるようにし、画面を離れられるようにする
```

### 8.4 Cron の使い道

Vercel Cron は Hobby で1日1回、**失敗しても UI に出ない**。学習機能を載せない。

| 用途 | Cron に載せるか |
|---|---|
| 1日1通の学習リマインド | ○ |
| YouTube の週次生存確認 | ○ |
| **先読み生成** | **✗**。ユーザーのアクセスを契機にする（§8.2） |
| ノルマの再計算 | ✗。ログイン時の遅延評価（`05-scheduler.md` §7） |

## 9. 未成年ユーザーとモデル提供元の規約

想定ユーザーはほぼ全員が未成年である。世界史は戦争・虐殺・宗教対立・植民地支配を扱うため、
**未成年向けの安全フィルタと衝突して生成が拒否される事故が起きうる**。

システムプロンプトに教育目的である旨を明記する（`07-content-pipeline.md` §5.1）。

**着工前に、次の10単元でプロンプト検証を行う（要検証）。**

```
十字軍 / アヘン戦争 / 三角貿易と奴隷制 / ナチス・ドイツとホロコースト /
インド大反乱 / アルメニア人問題 / 南京事件を含む日中戦争 / 太平洋戦争と原爆 /
アパルトヘイト / 文化大革命
```

**毎回生成では、拒否が起きたユーザーはその場で教材を得られない。**
共有カタログなら作者が事前に検知して人手執筆に切り替えられたが、その退路はない。
拒否された場合は「この単元の教材を生成できませんでした。教科書で学習してください」と出し、
`mode='self_study'` 相当に降格させる（設問だけは別プロンプトで再試行する）。

## 10. 実装前に検証すること

1. **Gemini API 無料枠の現行値**（RPM / RPD / TPM / TPD の有無 / 対象モデルID）
2. **無料枠に TPD 上限があるか**。あるなら 446,000 トークン/日 が収まるか（§3.3）
3. **代表20単元を生成し、年号・人名の誤り件数を人手で数える**（§5・最重要）
4. **`canon_event` による機械照合率**。`year` / `person` の claim の80%以上を照合できるか
5. §9 の10単元でモデルの拒否・婉曲が起きないか
5b. **検証モデルを Sonnet 5 と Haiku 系の両方で試し、検出率が同等なら安い方に落とす**（§2.2）
5c. **層3が実際に Flash の誤りを検出できるか**。既知の誤りを含む教材を作って検出率を測る
6. 1ユニットの生成に実際に何秒かかるか（Vercel の300秒に収まるか）
7. Gemini の構造化出力（JSON Schema 強制）が `07` §5.3 のスキーマで安定して通るか
8. 無料枠のデータ利用条件の原文（利用規約）を読み、§4.3 の文言と齟齬がないか
